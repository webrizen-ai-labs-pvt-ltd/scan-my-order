# shared assets

Store media that should be available to both apps here.

- `images/` for image files
- `videos/` for video files
- `audios/` for audio files

The backend serves this directory at `/assets`.
