import { CafeScene } from './cafe-scene';

/** Hero visual: the product in use, as an isometric 3D diorama. */
export function HeroStage({ className }) {
  return <CafeScene className={className} />;
}
