# Database Configuration
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/scan_my_order?schema=public"

# Server Configuration
PORT=8000
NODE_ENV=development

# JWT Configuration
JWT_SECRET="super-secret-jwt-key-scan-my-order-2026"
JWT_EXPIRES_IN="7d"

# OAuth Configuration
GOOGLE_CLIENT_ID="your-google-client-id"

# Passkey (WebAuthn) Configuration
RP_ID="localhost"
RP_NAME="Scan My Order"
ORIGIN="http://localhost:5173"

# Email Configuration (Nodemailer)
SMTP_HOST="smtp.mailtrap.io"
SMTP_PORT=2525
SMTP_USER="smtp-username"
SMTP_PASS="smtp-password"
EMAIL_FROM="noreply@scanmyorder.com"
